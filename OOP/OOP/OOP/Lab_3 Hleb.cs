/*using System;
using System.Text;

namespace OOP
{
    class Program
    {
        static void Main(string[] args)
        {
            double dblY;

            for (double a = 0; a <= 2; a += 0.5)
            {
                // Вивід значення a
                Console.WriteLine("\nЗначення параметра а = {0}", a);
                Console.WriteLine("--------------------------------------------------");

                for (double x = 0; x <= Math.PI; x += Math.PI / 36)
                {
                    dblY = Math.Pow(Math.E, -x * a) * Math.Sin(x);

                    Console.WriteLine("Аргумент х = {0, 3:f3}; Значення функції у = {1, 3:f3}", x, dblY);

                }
            }
            Console.Read();

*//*            Console.WriteLine("Введіть аргумент х: ");
            double dblX = double.Parse(Console.ReadLine());
            double dblS = dblX / 1;
            double dbLast;
            const double TOCHNOST = 1e-6;
            do
            {

            } while (dbLast > TOCHNOST);
*//*
        }
    }
}*/